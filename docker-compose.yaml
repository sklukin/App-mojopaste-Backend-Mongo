version: '3'
services:
  mojopaste:
    image: mojopate_mongo
    build: .
    ports:
      - "3000:8080"
    volumes:
      - ./lib:/app-mojopaste-master/lib
    environment:
      PASTE_BACKEND: Mongo
      PERLLIB: '/app-mojopaste-master/lib'
  mongo:
    image: mongo
    restart: always
